import React from 'react'

const ClearCup = (props) => (
    <div className='clearCupContainer'>
        <button className='clearCup' onClick={props.clicked}>Clear</button>

    </div>

    
)



export default ClearCup